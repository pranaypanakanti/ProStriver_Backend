services:
  ProStriver-api:
    image: pranaypanakanti/prostriver:latest
    container_name: prostriver-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SPRING_PROFILES_ACTIVE: api
      SERVER_PORT: 8080
    ports:
      - "8080:8080"